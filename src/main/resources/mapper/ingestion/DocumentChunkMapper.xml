<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.skyshift.cognitiveragengine.ingestion.mapper.DocumentChunkMapper">
    <insert id="insert" parameterType="com.skyshift.cognitiveragengine.ingestion.model.entity.DocumentChunkEntity" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO document_chunks (
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            start_position,
            end_position,
            ingestion_run_id,
            is_current,
            created_at,
            updated_at
        ) VALUES (
            #{groupId},
            #{documentId},
            #{chunkNumber},
            #{chunkText},
            cast(#{metadataJson} as jsonb),
            #{startPosition},
            #{endPosition},
            #{ingestionRunId},
            #{isCurrent},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <insert id="batchInsert" parameterType="list">
        INSERT INTO document_chunks (
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            start_position,
            end_position,
            ingestion_run_id,
            is_current,
            created_at,
            updated_at
        ) VALUES
        <foreach collection="list" item="chunk" separator=",">
            (
                #{chunk.groupId},
                #{chunk.documentId},
                #{chunk.chunkNumber},
                #{chunk.chunkText},
                cast(#{chunk.metadataJson} as jsonb),
                #{chunk.startPosition},
                #{chunk.endPosition},
                #{chunk.ingestionRunId},
                #{chunk.isCurrent},
                #{chunk.createdAt},
                #{chunk.updatedAt}
            )
        </foreach>
        ON CONFLICT (document_id, group_id, ingestion_run_id, chunk_number) DO NOTHING
    </insert>

    <insert id="batchInsertChunks" parameterType="list">
        INSERT INTO document_chunks (
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            ingestion_run_id,
            is_current,
            created_at,
            updated_at
        ) VALUES
        <foreach collection="chunks" item="chunk" separator=",">
            (
                #{chunk.groupId},
                #{chunk.documentId},
                #{chunk.chunkNumber},
                #{chunk.chunkText},
                cast(#{chunk.metadataJson} as jsonb),
                #{chunk.ingestionRunId},
                #{chunk.isCurrent},
                #{chunk.createdAt},
                #{chunk.updatedAt}
            )
        </foreach>
        ON CONFLICT (document_id, group_id, ingestion_run_id, chunk_number) DO NOTHING
    </insert>

    <delete id="deleteByDocumentId" parameterType="long">
        DELETE FROM document_chunks
        WHERE document_id = #{documentId}
    </delete>

    <delete id="deleteByDocumentIdAndGroupId">
        DELETE FROM document_chunks
        WHERE document_id = #{documentId}
        AND group_id = #{groupId}
    </delete>

    <delete id="deleteByIngestionRunId">
        DELETE FROM document_chunks
        WHERE ingestion_run_id = #{ingestionRunId}
    </delete>

    <update id="retireCurrentChunks">
        UPDATE document_chunks
        SET is_current = false, updated_at = NOW()
        WHERE document_id = #{documentId}
        AND group_id = #{groupId}
        AND is_current = true
    </update>

    <update id="promoteRunChunks">
        UPDATE document_chunks
        SET is_current = true, updated_at = NOW()
        WHERE ingestion_run_id = #{ingestionRunId}
    </update>

    <select id="selectByDocumentId" resultType="com.skyshift.cognitiveragengine.ingestion.model.entity.DocumentChunkEntity">
        SELECT
            id,
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            start_position,
            end_position,
            created_at,
            updated_at
        FROM document_chunks
        WHERE document_id = #{documentId}
        AND group_id = #{groupId}
        ORDER BY chunk_number ASC, id ASC
    </select>

    <select id="selectByDocumentIdAndGroupId" resultType="com.skyshift.cognitiveragengine.ingestion.model.entity.DocumentChunkEntity">
        SELECT
            id,
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            ingestion_run_id,
            is_current,
            created_at,
            updated_at
        FROM document_chunks
        WHERE document_id = #{documentId}
        AND group_id = #{groupId}
        AND is_current = true
        ORDER BY chunk_number ASC, id ASC
    </select>

    <select id="selectByDocumentIdAndGroupIdWithPagination" resultType="com.skyshift.cognitiveragengine.ingestion.model.entity.DocumentChunkEntity">
        SELECT
            id,
            group_id,
            document_id,
            chunk_number,
            chunk_text,
            metadata_json,
            created_at,
            updated_at
        FROM document_chunks
        WHERE document_id = #{documentId}
        AND group_id = #{groupId}
        ORDER BY chunk_number ASC, id ASC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByDocumentId" resultType="int">
        SELECT COUNT(*)
        FROM document_chunks
        WHERE document_id = #{documentId}
    </select>

</mapper>